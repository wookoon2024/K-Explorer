# K-Explorer v1.3.17 릴리즈 노트

**배포일**: 2026-07-21

## 주요 수정 및 추가 내용

### 1. 외부 편집기 설정 및 F4 실행 단축키 지원
- 파일 목록에서 **F4** 단축키를 눌러 선택한 파일을 즉시 열 수 있는 **외부 편집 프로그램 연결 기능**을 추가했습니다.
- **환경설정 -> 파일 작업** 탭에 **"외부 편집 프로그램 (F4)"** 경로를 설정하고 파일을 찾아볼 수 있는 브라우즈(`찾아보기...`) 기능을 지원합니다.
- 상단 메뉴바 **파일(F), 명령(C)** 및 마우스 우클릭 **컨텍스트 메뉴**에 **"편집 (F4)"** 동작을 추가해 UI 일관성을 확보했습니다.
- 직관적인 사용을 위해 다른 기본 단축키와 겹치거나 불필요했던 기존 **F5 (복사)**, **F6 (이동)** 단축키 바인딩 및 가이드를 제거했습니다.

### 2. 새 파일 생성 기능 추가
- 폴더 생성 외에 빈 0바이트 파일을 새로 생성할 수 있는 **새 파일 생성 기능**을 추가했습니다.
- 상단 메뉴 및 목록 우클릭 컨텍스트 메뉴에 **"새 파일"** 메뉴 아이템이 제공됩니다. 생성 후 해당 파일이 자동으로 선택 및 포커스되어 바로 작업이 가능합니다.

### 3. 이미지 호버 미리보기 설정 옵션화 (기본값 Off)
- 마우스 포인터를 이미지 파일 위에 올렸을 때 나타나던 이미지 팝업 미리보기 기능을 환경설정에서 **켜고 끌 수 있도록 옵션화**했습니다.
- **환경설정 -> 일반 -> 보기 설정**에 **"마우스 호버 시 이미지 미리보기 표시"** 체크박스가 추가되었으며, 시스템 리소스 절약 및 깔끔한 탐색을 위해 **기본값은 Off**로 변경되었습니다.

### 4. 환경설정 창 버튼 디자인 개선
- 환경설정 하단의 액션 버튼(`적용`, `확인`, `취소`) 디자인을 일관된 둥근 테두리(`CornerRadius="4"`) 스타일로 현대화했습니다.
- 주요 액션을 나타내는 **`확인`** 버튼은 기존 삭제 경고창 등과 동일하게 눈에 잘 띄는 **파란색(Primary Blue)** 테마를 기본으로 입혀 UI 완성도를 크게 높였습니다.

### 5. C 드라이브 루트 및 드라이브 재탐색 수정
- C 드라이브 등 이미 선택된 드라이브 버튼을 재클릭했을 때, 루트 경로(예: `C:\`)로 초기화 탐색이 되지 않던 캐시 갱신 및 입력 제어 동작을 수정했습니다.
- 드라이브 문자(예: `C:`)의 캐시 무효화 시 백슬래시 누락으로 발생하는 경로 버그를 수정하여 드라이브 루트에서의 디렉터리 이름 변경 시 화면 갱신이 지연되는 현상을 완전히 해결했습니다.

### 6. 다량 복사 작업 중 취소 시 프로그램 비정상 종료 수정
- 다량 파일 복사/이동 작업 도중 사용자가 취소(Cancel)를 눌렀을 때, 백그라운드 태스크에서 예외 처리가 누락되어 프로그램이 비정상적으로 종료되던 충돌 현상을 방지했습니다.

---

이전 버전의 릴리즈 노트는 저장소 커밋 기록에서 확인할 수 있습니다.

[메인 페이지로 돌아가기](README.md)
