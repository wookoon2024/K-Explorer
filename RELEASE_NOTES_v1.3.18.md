# K-Explorer v1.3.18 릴리즈 노트

**배포일**: 2026-07-21

## 주요 수정 및 추가 내용

### 1. 환경설정 카테고리 아이콘 시각화 개편
- 환경설정 창 좌측의 트리 뷰 카테고리에 고품질 컬러 이모지 아이콘 리소스(PNG)를 연동하여 시각적인 직관성과 디자인 감성을 강화했습니다.
- OS의 이모지 폰트 렌더링 방식에 무관하게 항상 깨지지 않고 선명한 고품질 컬러 아이콘을 제공합니다.

### 2. 데이터그리드 코너와 하단 빈 공간 분리 적용 및 개선
- 파일 목록 영역의 아래쪽 빈 공간은 원본 그대로 깔끔한 **검은색(ThemePanelBackground)**으로 복구했습니다.
- 동시에 스크롤바가 활성화될 경우 우측 최상단에 나타나는 코너 영역만 정확히 헤더 스타일과 어우러지는 **회색(ThemeHeaderBackground)**으로 분리 렌더링되도록 디자인을 다듬었습니다.
- 1픽셀 수준의 어긋남이 없도록 테두리선(BorderThickness) 오프셋을 세밀하게 반영했습니다.

### 3. 복사 및 붙여넣기 시 권한 예외 처리 도입 (안정성 개선)
- 복사/붙여넣기 진행 시 대상 디렉터리의 쓰기 권한 부족(예: 관리자 권한 미획득 상태로 `C:\Program Files` 하위에 붙여넣기) 등으로 오류(`Access Denied`)가 발생했을 때 프로그램 전체가 강제 종료되던 현상을 수정했습니다.
- 오류가 발생할 경우 프로세스가 다운되지 않고, 친절한 **작업 오류** 다이얼로그 창을 띄워 취소 또는 다음 동작을 처리할 수 있도록 안정성을 확보했습니다.

### 4. 안내 팝업 닫힘 시 탐색기 창 활성화 포커스 복원
- 복사/삭제 경고창, 작업 오류 창 등 모달(Modal) 창을 닫았을 때 탐색기 프로그램의 포커스를 잃고 브라우저 등 다른 앱 뒤로 프로그램이 숨겨지던 윈도우 활성화 버그를 수정했습니다.
- 모든 알림창이 닫히는 즉시 메인 탐색기 창을 앞으로 강제 복원(`Activate()`)하여 다이얼로그 확인 후 바로 탐색기를 이어서 조작할 수 있습니다.

### 5. 키보드 탐색 시 셀 포커스 테두리 제거
- 키보드로 파일/폴더 목록을 이동할 때 파일명 셀 주변에 어색하게 그려지던 검은색 점선 테두리(`FocusVisualStyle`)를 완벽히 제거하여 한결 정돈된 선택 뷰를 제공합니다.

### 6. 상위 폴더 삭제 시 예외 처리 개선
- 상위 폴더 이동용 `[..]` 행이 선택된 상태에서 불필요하게 삭제가 트리거되거나 활성 패널 포커스 백업 과정에서 오동작이 일어나는 문제를 보완했습니다.

---

이전 버전의 릴리즈 노트는 저장소 커밋 기록에서 확인할 수 있습니다.

[메인 페이지로 돌아가기](README.md)
