# K-Explorer v1.3.5 릴리즈 노트

**배포일**: 2026-07-19

이번 v1.3.5 업데이트에서는 사용자 피드백을 바탕으로 키보드 탐색 기능 강화와 함께 치명적인 포커스 버그를 해결하고 작업 취소 유연성을 대폭 개선했습니다.

## 주요 업데이트 내용

### 1. 🚀 다중 파일 선택 단축키 추가 (Shift + 방향키)
- 기존에는 마우스 드래그나 `Ctrl` + 클릭을 통해서만 다중 선택이 가능했지만, 이제 키보드만으로도 연속된 파일을 다중 선택할 수 있습니다.
- **사용 방법**: 목록에서 기준이 되는 파일을 선택한 뒤, `Shift` 키를 누른 상태에서 `위/아래/왼쪽/오른쪽 방향키`를 누르면 포커스가 이동하며 범위가 자동으로 선택됩니다.
- 자세히 보기 모드(`DataGrid`)와 타일/간단히 보기 모드(`ListBox`) 양쪽 모두에서 완벽하게 동작합니다.

### 2. 🛡️ 파일 작업 "모두 취소" 기능 지원
- 여러 파일을 한 번에 복사/이동/삭제할 때 파일 충돌 창이나 덮어쓰기 창이 나타나는 경우, 기존에는 현재 파일 1개만 건너뛰는 "취소(N)"만 있었습니다.
- 이제 **"모두 취소(C)"** 버튼이 추가되어, 더 이상 반복적으로 충돌 창이 뜨는 것을 막고 대기 중인 모든 파일 작업 큐를 한 번에 즉시 강제 종료할 수 있습니다.

### 3. 🎯 완벽한 구조적 패널 포커스 버그(Drift) 픽스
- **문제점**: 우측 패널에서 `Delete` 키를 이용해 파일을 삭제할 때, 찰나의 순간 동안 포커스가 좌측 패널로 넘어갔다가 돌아오는 깜빡임(Drift) 현상이 있었습니다.
- **해결**: WPF 내부의 포커스 복구 엔진(FocusManager)이 삭제된 요소를 처리하는 과정에서 발생하는 맹점을 발견하고 원천 차단했습니다. 삭제가 실행되는 찰나의 순간에 포커스를 윈도우 레이아웃 최상단의 보이지 않는 안전한 가상 공간(`FocusTrap`)에 대피시켰다가, 삭제 이후에 제자리로 완벽하게 되돌리는 방식을 적용하여 패널 간 포커스 간섭 현상을 완전히 종식시켰습니다.

---

[메인 페이지로 돌아가기](README.md)
