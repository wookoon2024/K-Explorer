# K-Explorer v1.3.19 릴리즈 노트

**배포일**: 2026-09-22

## 주요 수정 및 추가 내용

### 1. 프로그램 전체 글꼴 설정 (Pretendard 내장)
- 파일 목록 글꼴을 환경설정에서 바꿀 수 있고, 메뉴·탭·검색·메모 편집·환경설정·대화상자까지 앱 전체에 같은 글꼴이 적용됩니다.
- 무료로 배포할 수 있는 Pretendard(SIL OFL 1.1) Regular/Bold를 앱에 내장해 별도 설치 없이 사용할 수 있습니다. 라이선스 원문은 배포 파일의 `Assets\Fonts\LICENSE-Pretendard.txt`에 포함되어 있습니다.

### 2. 환경설정 화면 개편
- 카드형 레이아웃으로 정돈하고, 버튼을 즐겨찾기 파일 버튼과 같은 칩 스타일로 통일했습니다. (이모지 없이 정리)
- 충돌 처리, 기본 검색 범위 등 선택 항목을 한글 라벨로 표시합니다. 저장되는 값은 기존과 동일해 이전 설정과 호환됩니다.
- `속성` 열 표시를 켜고 끌 수 있어, 항목이 많은 폴더에서 목록이 가벼워집니다.

### 3. 이미지 뷰어 개선
- 좌측에 **파일목록** 사이드바를 추가했습니다. 현재 폴더의 이미지 파일만 보여주고 지금 보고 있는 파일을 강조하며, 오른쪽 가장자리의 `❮ / ❯` 손잡이로 열고 닫습니다. 열림 상태는 저장됩니다.
- 이미지를 두 번 누르면 이미지만 전체화면으로 보여주고, 하단 썸네일에서 현재 이미지를 표시합니다.
- 확대/축소는 **창에 맞춤**(Ctrl+0)과 **원본 크기**(Ctrl+1) 두 가지만 제공하고, 현재 모드가 체크 표시됩니다. 창에 맞춤은 이미지가 잘리지 않게 창 안에 꽉 차도록 맞춥니다.
- DPI 정보가 있는 사진(예: 300DPI)이 지나치게 작게 보이던 문제를 수정했고, 창을 최대화해도 맞춤이 다시 계산됩니다.
- 자동 넘김(슬라이드쇼)은 기본 OFF이며 `Ctrl+Space`로만 시작합니다.
- 이미지 크기 조정, 스탬프(파일명/날짜) 기능을 추가했습니다.

### 4. 단축키 변경 기능
- 파일 열기/편집/삭제/이름 바꾸기/새 폴더 등 주요 명령의 단축키를 사용자가 직접 바꿀 수 있습니다.

### 5. 상단 바와 버튼 디자인 통일
- 탐색기 상단 버튼과 아이콘, 즐겨찾기 바(즐겨찾기 파일, Downloads 등)를 칩 스타일로 다시 그려 통일했습니다. 폴더 끌어놓기 등 기존 동작은 그대로입니다.
- 메시지 상자, 새 폴더, 이름 바꾸기, 압축 진행 등 모든 대화상자 버튼을 같은 스타일로 맞췄습니다.

### 6. 탭 사용성 개선
- 탭 우클릭 메뉴를 정돈하고 **모든 탭 닫기**를 추가했습니다.
- 새 탭과 4패널 탭이 현재 탭의 보기 방식(목록/상세)을 그대로 물려받습니다.
- 시작할 때 마지막으로 보던 보기 방식으로 열리고, 툴바 표시와 실제 목록이 어긋나던 문제를 수정했습니다.
- 세션 복원 시 탭 제목이 `L1`, `L2`로 남던 문제를 수정해 항상 폴더 이름으로 표시합니다.

### 7. 실행 및 폴더 로딩 속도 개선
- 화면에 보이는 탭만 실제로 읽도록 지연 로딩을 적용하고, 파일 변경 감시가 불필요하게 목록을 다시 그리지 않도록 억제했습니다.
- 시작 로딩과 `C:\Users\...`처럼 항목이 많은 폴더 진입이 눈에 띄게 빨라졌습니다.

### 8. 그 외 수정
- 삭제 직후 선택이 튀던 문제와 충돌 대화상자의 Z순서·창 활성화 문제를 정리했습니다.
- 2패널 모드에서 어느 쪽이 선택된 패널인지 드라이브 `C:` 행 배경으로 표시합니다.

---

이전 버전의 릴리즈 노트는 저장소 커밋 기록에서 확인할 수 있습니다.

[메인 페이지로 돌아가기](README.md)
