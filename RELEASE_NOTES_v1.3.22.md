# K-Explorer v1.3.22 릴리즈 노트

**배포일**: 2026-09-25

## 주요 수정 및 개선 내용

### 1. 탭 이동 속도 대폭 개선
- 2패널의 각 탭이 실제 `DataGrid`/`ListBox` 화면을 유지하도록 탭별 화면 캐시를 적용했습니다.
- 이미 예열된 탭은 `ItemsSource`를 다시 교체하지 않고 `Hidden`과 `Visible`만 전환합니다.
- 현재 탭의 이전·다음 탭은 유휴 시간에 화면과 목록 데이터를 미리 준비합니다.
- 좌우 패널을 합쳐 최대 8개 탭 화면을 LRU로 유지하며, 닫힌 탭 화면은 즉시 정리합니다.
- 예열된 탭 측정 결과는 왼쪽 40/40회, 오른쪽 40/40회 모두 100ms 이하였으며 예외는 없었습니다.

### 2. 탭별 선택·스크롤·정렬 상태 유지
- 각 탭의 다중 선택, 상세/목록/간단목록 보기 상태를 유지합니다.
- 상세 보기 정렬 방향, 현재 셀, 가로·세로 스크롤 위치를 탭별로 보존합니다.
- 예열된 비활성 탭의 목록 변경 후에도 이전 선택과 스크롤 위치를 복원합니다.

### 3. 파일 추가·삭제·이름 변경 반영 개선
- 파일 목록에 파일이 추가·삭제·이름 변경되면 현재 패널 목록을 즉시 다시 계산합니다.
- 비활성 탭과 4패널의 모든 탭도 파일 변경 감시 대상에 포함합니다.
- 외부 파일 이벤트의 고정 대기 시간을 줄여 watcher 반영 지연을 낮췄습니다.
- 감시 중이던 폴더 자체가 삭제되면 존재하는 상위 폴더로 자동 이동해 오래된 목록이 남지 않게 했습니다.

### 4. 기존 파일 작업과 4패널 동작 유지
- 내부 복사·이동·붙여넣기·삭제 후 시작 폴더와 대상 폴더를 모두 갱신합니다.
- 자기 작업으로 인한 중복 watcher 갱신은 억제해 목록 깜빡임과 재바인딩을 줄였습니다.
- 4패널 전환과 2패널 복귀를 포함한 기존 모드 전환 동작을 유지했습니다.

### 5. 성능 계측 보강
- 탭 전환 시작, 처리 완료, 실제 첫 프레임 시간을 구분해 기록하도록 개선했습니다.
- 예열된 탭의 실제 첫 프레임 시간을 기준으로 성능을 확인할 수 있게 했습니다.

---

이전 버전의 릴리즈 노트는 저장소 커밋 기록에서 확인할 수 있습니다.

[메인 페이지로 돌아가기](README.md)
