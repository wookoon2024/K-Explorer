<Window x:Class="WorkFileExplorer.App.Dialogs.TransferProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="파일 작업"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FCFCFD"
        FontFamily="Malgun Gothic">
    <Grid Margin="16,14,16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="OperationText"
                   Grid.Row="0"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Foreground="#1F2937"
                   Text="처리 중..." />

        <ProgressBar x:Name="ProgressBarControl"
                     Grid.Row="1"
                     Height="14"
                     Margin="0,10,0,0"
                     Minimum="0"
                     Maximum="1"
                     Foreground="#2F6FED" />

        <TextBlock x:Name="CurrentFileText"
                   Grid.Row="2"
                   Margin="0,8,0,0"
                   FontSize="11"
                   Foreground="#6B7280"
                   TextTrimming="CharacterEllipsis"
                   Text=" " />

        <Button x:Name="CancelButton"
                Grid.Row="3"
                Style="{StaticResource DialogButtonStyle}"
                HorizontalAlignment="Right"
                Margin="0,12,0,0"
                MinWidth="84"
                Content="취소(_N)"
                Click="OnCancelClick" />
    </Grid>
</Window>
