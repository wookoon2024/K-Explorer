<Window x:Class="WorkFileExplorer.App.Dialogs.ItemMemoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="메모 편집"
        Width="520"
        Height="300"
        MinWidth="460"
        MinHeight="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#F4F4F4"
        FontFamily="Malgun Gothic">
    <Window.Resources>
        <Style x:Key="BottomButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#B6B6B6" />
            <Setter Property="Background" Value="#F5F5F5" />
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#EBEEF3" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#E0E6EF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="선택 항목 메모"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="#1F1F1F"
                   Margin="0,0,0,6" />

        <TextBlock Grid.Row="1"
                   Text="{Binding TargetPath}"
                   TextTrimming="CharacterEllipsis"
                   Foreground="#525252"
                   Margin="0,0,0,8" />

        <Border Grid.Row="2"
                BorderBrush="#B6B6B6"
                BorderThickness="1"
                CornerRadius="4"
                Background="White"
                Padding="4">
            <Border BorderBrush="#D3D7DF"
                    BorderThickness="1"
                    CornerRadius="3"
                    Background="#FCFCFD">
                <TextBox x:Name="MemoTextBox"
                         Text="{Binding MemoText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Padding="8"
                         BorderThickness="0"
                         Background="Transparent" />
            </Border>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Width="84"
                    Margin="0,0,8,0"
                    Style="{StaticResource BottomButtonStyle}"
                    Content="삭제"
                    Click="OnDeleteClick" />
            <Button Width="84"
                    Margin="0,0,8,0"
                    Style="{StaticResource BottomButtonStyle}"
                    Background="#E8F0FD"
                    BorderBrush="#8CAEE8"
                    Content="저장"
                    IsDefault="True"
                    Click="OnSaveClick" />
            <Button Width="84"
                    Style="{StaticResource BottomButtonStyle}"
                    Content="취소"
                    IsCancel="True"
                    Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</Window>
