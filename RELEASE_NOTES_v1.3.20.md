# K-Explorer v1.3.20 릴리즈 노트

**배포일**: 2026-09-22

## 주요 수정 및 추가 내용

### 1. 명령 프롬프트 버튼과 시작 명령 설정
- 상단 툴바에 **명령 프롬프트** 버튼을 추가했습니다. 누르면 활성 패널의 현재 폴더에서 cmd 창이 열립니다.
- 환경설정에 **명령 프롬프트 (cmd)** 항목을 넣어, 창을 열 때 자동으로 실행할 명령을 한 줄씩 적어 둘 수 있습니다.
- 여러 줄을 넣으면 한 창에서 순서대로 실행되므로, `set` 으로 지정한 환경 변수가 다음 줄 명령에 그대로 적용됩니다.
- 시작 명령에 `set 이름=값` 을 넣었을 때 값 뒤에 공백이 붙어(`0 ` 처럼) 제대로 전달되지 않던 문제를 수정했습니다.

### 2. Windows 탐색기에서 파일 끌어놓기
- Windows 탐색기에서 파일·폴더를 끌어다 K-Explorer 패널에 놓으면 현재 폴더로 복사됩니다. 폴더 위에 놓으면 그 폴더 안으로 들어갑니다. (4패널 슬롯도 동일)
- 원본은 그대로 두고 **복사만** 하므로, 끌어온 파일이 사라질 걱정이 없습니다.

### 3. Windows 클립보드 붙여넣기
- Windows 탐색기에서 파일을 복사(Ctrl+C)한 뒤 K-Explorer에서 붙여넣기(Ctrl+V)하면 활성 패널의 현재 폴더로 복사됩니다.
- 앱 안에서 복사/잘라내기 한 경우는 기존과 동일하게 동작하며, Shift와 함께 쓰면 이동으로 처리됩니다.

### 4. 그 외
- 창 제목에 버전이 표시됩니다. (`K탐색기 (v1.3.20)`)
- 프로그램 버전은 빌드 시 지정한 정보에서 읽어 오므로, 앞으로 버전을 올리면 제목도 자동으로 따라갑니다.

---

이전 버전의 릴리즈 노트는 저장소 커밋 기록에서 확인할 수 있습니다.

[메인 페이지로 돌아가기](README.md)
