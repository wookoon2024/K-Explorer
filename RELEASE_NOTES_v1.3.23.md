# K-Explorer v1.3.23 릴리즈 노트

**배포일**: 2026-09-26

## 주요 수정 및 개선 내용

### 1. 파일 검색(Find Files) 성능 및 반응 속도 대폭 개선
- 파일 검색 시 OS 파일 시스템 메타데이터 캐시를 활용하는 `DirectoryInfo.EnumerateFiles` 방식으로 최적화하여 대용량 폴더 순회 속도를 극대화했습니다.
- 파일 마스크(와일드카드), 파일 크기, 수정일 필터를 텍스트 내용 조회 전에 먼저 평가하는 조기 필터링(Early Exit)을 적용해 불필요한 디스크 I/O를 원천 차단했습니다.
- 검색 결과 목록 바인딩에 `RangeObservableCollection.AddRange` 일괄 삽입(Batching)을 적용하여, 수많은 결과가 반환될 때도 UI 스레드 렌더링 지연 및 버벅임을 방지했습니다.
- 검색어 입력 및 필터 변경 시 반응 지연 없이 즉각적인 검색 결과를 표시하도록 개선했습니다.

### 2. 환경설정(Settings) UI 정돈 및 가독성 개선
- 이미지 뷰어 설정 섹션 내 연결 확장자 체크박스 그리드 레이아웃을 정돈하여 컬럼 간격과 텍스트 시인성을 개선했습니다.
- 각 설정 항목별 명확한 툴팁 설명을 보강하여 옵션의 동작을 쉽게 이해할 수 있도록 했습니다.

---

이전 버전의 릴리즈 노트는 저장소 커밋 기록에서 확인할 수 있습니다.

[메인 페이지로 돌아가기](README.md)
